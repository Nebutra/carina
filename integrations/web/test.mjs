import assert from 'node:assert/strict'; import fs from 'node:fs'; const html=fs.readFileSync(new URL('index.html',import.meta.url),'utf8'); const js=fs.readFileSync(new URL('app.js',import.meta.url),'utf8'); assert.match(html,/Enable write controls/); assert.match(js,/remote gateways require WSS/); assert.match(js,/showModal/); assert.match(js,/gateway\.hello/); console.log('web operator smoke: ok')
