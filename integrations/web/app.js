const $=s=>document.querySelector(s);let ws,next=0,tab='agents',pending=new Map(),granted=[],methods=new Map(),reconnectTimer,backoff=250,intentional=false,streams=new Map(),cursors=new Map(),snapshot,stale=true,refreshTimer,catchups=new Map()
const routes={workflows:'workflow.list',cost:'usage.cost'}
const TIMELINE_PAGE_SIZE=100,MAX_TIMELINE_PAGES=20,MAX_ARTIFACT_BYTES=2*1024*1024
function setConnection(ok,text,isStale=false){stale=isStale;$('#connection').className=ok?'online':'offline';$('#connection').textContent=text}
function call(method,params={}){return new Promise((resolve,reject)=>{if(!ws||ws.readyState!==1)return reject(new Error('Gateway is disconnected'));const id=++next;const timer=setTimeout(()=>{if(pending.delete(id))reject(new Error(`${method} timed out`))},15000);pending.set(id,{resolve,reject,timer});ws.send(JSON.stringify({jsonrpc:'2.0',id,method,params}))})}
function gatewayConfig(){const write=$('#writes').checked;return{url:$('#url').value.trim(),token:$(write?'#operatorToken':'#observerToken').value,role:write?'operator':'observer',scopes:write?['read','stream','write','admin']:['read','stream']}}
function validURL(url){return /^wss:\/\//.test(url)||/^ws:\/\/(127(?:\.\d+){3}|\[::1\])(?::\d+)?\//.test(url)}
function connect(){intentional=true;ws?.close();intentional=false;clearTimeout(reconnectTimer);const c=gatewayConfig();if(!validURL(c.url))return setConnection(false,'Refused: remote gateways require WSS',true);setConnection(false,`Connecting as ${c.role}…`,true);const socket=new WebSocket(c.url);ws=socket;socket.onopen=async()=>{try{const hello=await call('gateway.hello',{protocol_version:1,client_id:'carina-web',role:c.role,scopes:c.scopes,token:c.token});granted=hello.scopes||[];await call('runtime.initialize',{protocol_version:'1.2.0',schema_version:'1.2.0',client_name:'carina-web',client_version:'0.1.0'});const descriptors=await call('gateway.methods');methods=new Map((descriptors.methods||[]).map(x=>[x.method,x]));backoff=250;streams.clear();setConnection(true,`Connected · ${c.role} · ${granted.join(', ')}`);await resync()}catch(e){setConnection(false,e.message,true);socket.close()}};socket.onmessage=e=>{let m;try{m=JSON.parse(e.data)}catch{return}if(m.id!==undefined){const p=pending.get(m.id);if(p){pending.delete(m.id);clearTimeout(p.timer);m.error?p.reject(new Error(m.error.message)):p.resolve(m.result)}}else if(m.method==='event')onEvent(m.params)};socket.onclose=()=>{if(ws!==socket)return;setConnection(false,'Stale · reconnecting',true);streams.clear();for(const p of pending.values()){clearTimeout(p.timer);p.reject(new Error('Gateway disconnected'))}pending.clear();if(!intentional)scheduleReconnect()};socket.onerror=()=>setConnection(false,'Connection failed',true)}
function scheduleReconnect(){clearTimeout(reconnectTimer);const delay=backoff;backoff=Math.min(backoff*2,10000);reconnectTimer=setTimeout(connect,delay)}
function scheduleResync(){if(refreshTimer)return;refreshTimer=setTimeout(()=>{refreshTimer=undefined;void resync()},150)}
function onEvent(event){if(!event||typeof event!=='object')return;if(event.session_id)scheduleCatchUp(event.session_id);if(/Task|Session|task\.completed|approval|question/i.test(event.type||''))scheduleResync()}
function scheduleCatchUp(sessionId){if(catchups.has(sessionId))return;catchups.set(sessionId,setTimeout(async()=>{catchups.delete(sessionId);try{const result=await call('session.attach',{session_id:sessionId,since:cursors.get(sessionId)||0});cursors.set(sessionId,result.cursor)}catch{}},100))}
async function resync(){snapshot=await call('agent.view');const sessions=[...snapshot.needs_input,...snapshot.working,...snapshot.completed],live=new Set(sessions.map(a=>a.session_id));for(const[sessionId,stream]of streams){if(live.has(sessionId))continue;if(stream.id&&canRemote('session.events.unsubscribe'))void call('session.events.unsubscribe',{subscription_id:stream.id}).catch(()=>{});streams.delete(sessionId);cursors.delete(sessionId)}for(const a of sessions){if(streams.has(a.session_id))continue;try{const result=await call('session.events.stream',{session_id:a.session_id,since:cursors.get(a.session_id)||0}),cursor=result.cursor??cursors.get(a.session_id)??0;cursors.set(a.session_id,cursor);streams.set(a.session_id,{id:result.subscription_id,cursor})}catch(e){if(!/scope|remote|method/i.test(e.message))throw e}}await render()}
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const rowsOf=v=>Array.isArray(v)?v:(v?.providers||[])
async function render(){const out=$('#content');out.innerHTML='<p class="muted">Loading…</p>';try{if(!snapshot&&(tab==='agents'||tab==='inbox'))snapshot=await call('agent.view');if(tab==='agents'){const sections=[['Needs input',snapshot.needs_input,'needs'],['Working',snapshot.working,''],['Completed',snapshot.completed,'']];out.innerHTML=sections.map(([name,rows,klass])=>`<h2>${name} <small>${rows.length}</small></h2>${rows.length?rows.map(x=>agentRow(x,klass)).join(''):'<p class="muted">None</p>'}`).join('');return}if(tab==='inbox'){out.innerHTML=`<h2>Needs input <small>${snapshot.needs_input.length}</small></h2>${snapshot.needs_input.length?snapshot.needs_input.map(x=>agentRow(x,'needs')).join(''):'<p class="muted">No approvals, questions, conflicts, or policy blocks.</p>'}`;return}if(tab==='system'){const[status,workers,extensions]=await Promise.all([call('daemon.status'),call('worker.list'),call('extension.list')]);out.innerHTML=`<h2>Runtime</h2><pre>${esc(JSON.stringify(status,null,2))}</pre><h2>Workers</h2><pre>${esc(JSON.stringify(workers,null,2))}</pre><h2>Extensions</h2><pre>${esc(JSON.stringify(extensions,null,2))}</pre>`;return}const rows=rowsOf(await call(routes[tab]));out.innerHTML=rows.length?rows.map(x=>`<div class="row"><strong>${esc(x.workflow||x.provider||x.id)}</strong><span>${esc(x.status||x.model||'')}</span><span>${esc(x.cost_usd!=null?'$'+Number(x.cost_usd).toFixed(4):x.progress!=null?Math.round(x.progress*100)+'%':'')}</span><span></span></div>`).join(''):'<p class="muted">No records.</p>'}catch(e){out.innerHTML=`<p class="error">${esc(e.message)}. Showing no authoritative snapshot; check daemon, token expiry, role, and scopes.</p>`}}
function agentRow(x,klass){return`<div class="row ${klass} ${stale?'stale':''}"><strong>${esc(x.title||x.summary||x.session_id)}</strong><span>${esc(stale?'Stale':x.state)}</span><span>${esc(x.workspace_root||x.summary||'')}</span><button data-inspect="${esc(x.session_id)}">Inspect</button></div>`}
function actionFrom(events){let decision,question,diffs=[];for(const e of events){const p=e.payload||{};if(typeof p.diff==='string')diffs.push(p.diff);const d=e.permission_decision_id||p.decision_id;if(d&&(String(e.type).match(/approval|permission/i)||p.status==='approval_requested'))decision=d;if(p.status==='approval_resolved')decision=undefined;if(p.question_id)question={id:p.question_id,prompt:p.prompt||p.question||'Question',options:Array.isArray(p.options)?p.options:[]};if(p.status==='user_question_resolved')question=undefined}return{decision,question,diffs}}
function canRemote(method){const d=methods.get(method);return Boolean(d?.remote&&granted.includes(d.scope))}
async function sessionItems(sessionId){let cursor,data=[];for(let i=0;i<MAX_TIMELINE_PAGES;i++){const page=await call('session.items',{session_id:sessionId,limit:TIMELINE_PAGE_SIZE,...(cursor?{cursor}:{})});data.push(...(page.data||[]));if(!page.next_cursor)return{data,truncated:false};cursor=page.next_cursor}return{data,truncated:true}}
function timelineRows(items,filter='all'){let turn='';return items.flatMap(e=>{const next=e.turn_id||e.task_id||e.item?.task_id||'',item=e.item,d=e.details||item?.details||{},status=item?.status||String(d.status||'changed'),kind=e.type==='runtime.stage_changed'?'stage':item?.type||e.type;if(filter!=='all'&&status!==filter)return[];const heading=next&&next!==turn?(turn=next,`<li class="turn"><strong>Turn ${esc(turn)}</strong></li>`):'';const callId=d.call_id||e.item_id||item?.id||'',artifacts=[d.artifact_id,...(Array.isArray(d.artifact_ids)?d.artifact_ids:[])].filter(x=>typeof x==='string'&&x);return[heading+`<li class="timeline-item status-${esc(status)}" id="call-${esc(callId)}"><span class="timeline-kind">${esc(kind)}</span><strong>${esc(status)}</strong><span>${esc(d.stage||d.tool||callId||'')}</span>${artifacts.map(id=>`<button data-artifact="${esc(id)}" data-session="${esc(e.session_id)}">Artifact</button>`).join('')}</li>`]}).join('')}
async function inspect(sessionId){const[{data:items,truncated},review,attached]=await Promise.all([sessionItems(sessionId),call('session.review',{session_id:sessionId}),call('session.attach',{session_id:sessionId,since:Math.max(0,(cursors.get(sessionId)||0)-200)})]),a=actionFrom(attached.events||[]),write=$('#writes').checked;let actions='';if(a.decision)actions+=canRemote('task.approval.resolve')&&write?`<button data-approve="${esc(a.decision)}">Resolve approval</button>`:'<span class="muted">Approval pending · resolve in a local client (remote RPC unavailable)</span>';if(a.question)actions+=canRemote('task.user.answer')&&write?`<button data-answer="${esc(a.question.id)}" data-prompt="${esc(a.question.prompt)}">Answer</button>`:'<span class="muted">Question pending · answer in a local client (remote RPC unavailable)</span>';$('#content').innerHTML=`<button id="back">Back</button><h2>Session ${esc(sessionId)}</h2><p><strong>${esc(review.state)}</strong> · projection ${esc(review.projection_version)}</p><p>${esc(review.summary||review.waiting_reason||'')}</p>${actions}<section class="timeline-band" aria-labelledby="timelineTitle"><div class="timeline-head"><h3 id="timelineTitle">Authoritative timeline</h3><label>Status <select id="timelineFilter"><option value="all">All</option><option value="running">Running</option><option value="completed">Completed</option><option value="failed">Failed</option><option value="denied">Denied</option></select></label></div>${truncated?'<p class="muted">Timeline is bounded; newer pages beyond the client cap are omitted.</p>':''}<ol id="timelineList" class="timeline">${timelineRows(items)}</ol></section>`;$('#back').onclick=render;$('#timelineFilter').onchange=e=>{$('#timelineList').innerHTML=timelineRows(items,e.target.value)}}
async function openArtifact(sessionId,artifactId){const dialog=$('#artifact'),body=$('#artifactBody'),meta=$('#artifactMeta');$('#artifactTitle').textContent=`Artifact ${artifactId}`;body.textContent='Loading…';meta.textContent='';$('#artifactDownload').disabled=true;dialog.showModal();if(!canRemote('artifact.read')){body.textContent='Artifact content is local-only and cannot be opened through this Gateway. Use a local Carina client.';return}let offset=0,total=0,chunks=[],metadata;for(;;){const page=await call('artifact.read',{session_id:sessionId,artifact_id:artifactId,offset,limit:65536});metadata=page.metadata;const bytes=Uint8Array.from(atob(page.content_base64),c=>c.charCodeAt(0));total+=bytes.length;if(total>MAX_ARTIFACT_BYTES)throw new Error('Artifact exceeds the web preview limit.');chunks.push(bytes);if(page.eof)break;if(page.next_offset<=offset)throw new Error('Artifact pagination did not advance.');offset=page.next_offset}const blob=new Blob(chunks,{type:metadata.media_type||'application/octet-stream'}),textual=metadata.preview_utf8!==false&&(!metadata.media_type||metadata.media_type.startsWith('text/')||/json|xml|yaml|javascript/.test(metadata.media_type));meta.textContent=`${metadata.media_type||'application/octet-stream'} · ${total} bytes`;body.textContent=textual?await blob.text():'Binary artifact is available for download; inline rendering is disabled.';const download=$('#artifactDownload');download.disabled=false;download.onclick=()=>{const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=artifactId;a.click();setTimeout(()=>URL.revokeObjectURL(url),0)}}
async function confirmed(method,params){if(!$('#writes').checked)throw new Error('Operator mode is disabled');if(!canRemote(method))throw new Error(`${method} is not exposed for remote Gateway writes`);$('#operation').textContent=JSON.stringify({method,params},null,2);$('#confirm').showModal();if(await new Promise(r=>{const d=$('#confirm');d.addEventListener('close',()=>r(d.returnValue==='default'),{once:true})}))return call(method,params)}
function selectTab(b){document.querySelector('.tab.active')?.classList.remove('active');document.querySelectorAll('.tab').forEach(x=>{x.setAttribute('aria-selected',String(x===b));x.tabIndex=x===b?0:-1});b.classList.add('active');tab=b.dataset.tab;render()}
$('#settings').onclick=()=>$('#connect').showModal();$('#connectNow').onclick=connect;$('#refresh').onclick=()=>resync();document.querySelectorAll('.tab').forEach(b=>{b.onclick=()=>selectTab(b);b.onkeydown=e=>{if(!['ArrowDown','ArrowUp','ArrowRight','ArrowLeft','Home','End'].includes(e.key))return;e.preventDefault();const tabs=[...document.querySelectorAll('.tab')],i=tabs.indexOf(b),next=e.key==='Home'?0:e.key==='End'?tabs.length-1:(i+(e.key==='ArrowDown'||e.key==='ArrowRight'?1:-1)+tabs.length)%tabs.length;tabs[next].focus();selectTab(tabs[next])}});$('#content').onclick=async e=>{const d=e.target.dataset||{};try{if(d.inspect)await inspect(d.inspect);if(d.artifact)await openArtifact(d.session,d.artifact);if(d.approve)await confirmed('task.approval.resolve',{decision_id:d.approve,approve:true,scope:'once',approver:'web'});if(d.answer){const value=prompt(d.prompt);if(value!==null)await confirmed('task.user.answer',{question_id:d.answer,value})}}catch(err){alert(err.message)}};$('#writes').onchange=()=>{$('#modeHelp').textContent=$('#writes').checked?'Operator token · server scopes still authoritative':'Read-only observer';connect()};$('#connect').showModal()
export{esc,validURL}
