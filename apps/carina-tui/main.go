// carina-tui is the interactive terminal UI for Carina (Phase 1+).
package main

import "fmt"

func main() {
	fmt.Println("carina-tui: interactive TUI lands in Phase 1 — use `pi` (apps/carina-cli) for now")
}
