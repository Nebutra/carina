import assert from 'node:assert/strict'; import fs from 'node:fs'; const p=JSON.parse(fs.readFileSync(new URL('package.json',import.meta.url))); assert.equal(p.version,'0.6.1'); assert.equal(Object.keys(p.optionalDependencies).length,4); assert.match(fs.readFileSync(new URL('bin/carina.js',import.meta.url),'utf8'),/optional dependencies enabled/); console.log('npm launcher smoke: ok')
