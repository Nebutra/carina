# Carina 0.8.13

Patch release. Unblocks the 0.8.12 tag workflow after release-gate TUI
contract tests failed, and carries the same end-to-end effort honesty work.

## Highlights

### Release-gate TUI contract compliance

- Provider status column layout uses named `layout_contract` constants (no bare
  `saturating_add(1)` / glyph literals in product sources).
- Keeps the 0.8.12 inventory↔wire effort contract, stale-effort remap, and
  localized failure titles.

## Upgrade notes

- Prefer `0.8.13` over `0.8.11` / `0.8.12` tags if those workflows did not
  finish publishing.
- Restart `carina-daemon` and relaunch the TUI after upgrade.
- Standalone: `carina update`.
